import * as Styled from './styles';

const Logo = () => {
  return (
    <Styled.Wrapper>
      <img src="img/logo.jpg" alt="Logo" aria-label="Logo" />
    </Styled.Wrapper>
  );
};

export default Logo;
