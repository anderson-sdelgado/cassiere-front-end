import { screen } from '@testing-library/react';
import { renderTheme } from '../../utils/render-theme';
import {{pascalCase name}}, { {{pascalCase name}}TemplateProps } from '.';

const props: {{pascalCase name}}TemplateProps = {
  children: 'any',
};

describe('<{{pascalCase name}} />', () => {
  it('should render', () => {
    renderTheme(<{{pascalCase name}} {...props} />);
    expect(screen.getByText('any')).toBeInTheDocument();
  });
});
